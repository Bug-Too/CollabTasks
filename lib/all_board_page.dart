import 'package:flutter/material.dart';
class AllBoardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Board'),
      ),
      body: Center(
        child: Text('All Board'),
      ),
    );
  } 
}